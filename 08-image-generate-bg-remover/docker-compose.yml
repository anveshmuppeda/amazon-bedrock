version: '3.8'

services:
  image_generate_ui:
    build: .
    ports:
      - "8501:8501"
    environment:
      - AWS_DEFAULT_REGION=us-east-1
      - AWS_REGION=us-east-1
    restart: unless-stopped